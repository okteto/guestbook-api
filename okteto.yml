build:
  web:
    context: .

deploy:
  compose: docker-compose.yml

dev:
  web:
    command: bash
    volumes:
    - /root/.cache/pip
    sync:
    - .:/app
    forward:
    - 8080:8080
    reverse:
    - 9000:9000
